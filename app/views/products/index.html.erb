<h1>Products</h1>

Our store has <%= Product.public_count %> products and counting!

<ul>
    <% @products.each do |product| %>
        <% unless product.archived? %>
            <li>
                <%= link_to product.title, product %>
            </li>
        <% end %>
    <% end %>
</ul>

<%= link_to "New product", new_product_path %>